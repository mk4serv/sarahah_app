// import * as controllers from '../index.js';

// const controllerRouter = (app, express) => {
    // ✅ Routes Prefix
   // app.use("/auth", controllers.authController);
    // app.use("/user", controllers.userController);
    // app.get("/", (req, res) => res.send("Hello from the root route"));
// };